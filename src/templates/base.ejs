<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title><%= title || 'Email from Your Company' %></title>
  <!--[if mso]>
    <noscript>
        <xml>
            <o:OfficeDocumentSettings>
                <o:PixelsPerInch>96</o:PixelsPerInch>
            </o:OfficeDocumentSettings>
        </xml>
    </noscript>
    <![endif]-->
  <style>
    /* Dynamic styles */
    /* <%= styles %> */

    /* It's recommended to use inline styles for email compatibility instead of Tailwind */
    /* Email clients strip out <style> tags and external stylesheets */
    /* Keep the original email-specific styles */
    body,
    table,
    td,
    p,
    a,
    li,
    blockquote {
      -webkit-text-size-adjust: 100%;
      -ms-text-size-adjust: 100%;
    }

    table,
    td {
      margin-left: 0;
      margin-right: 0;
    }

    img {
      -ms-interpolation-mode: bicubic;
      border: 0;
      height: auto;
      line-height: 100%;
      outline: none;
      text-decoration: none;
    }

    @media screen and (max-width: 600px) {
      .email-container {
        width: 100% !important;
        margin: 0 !important;
      }

      .mobile-padding {
        padding: 20px !important;
      }

      .mobile-text-center {
        text-align: center !important;
      }
    }
  </style>
</head>

<body style="margin: 0; padding: 0; background-color: #f3f4f6; font-family: Arial, Helvetica, sans-serif;">
  <table role="presentation" cellspacing="0" cellpadding="0" border="0" width="100%">
    <tr>
      <td style="padding: 20px 0;">
        <div style="max-width: 600px; margin: 0 auto; padding: 0 20px;">
          <!-- Header -->
          <div style="text-align: center; padding: 20px 0;">
            <% if (logo) { %>
            <img src="<%= logo %>" alt="<%= companyName %>" style="height: 40px; margin-bottom: 10px;">
            <% } %>
            <h1 style="margin: 0; font-size: 24px; font-weight: bold; color: #1f2937;"><%= companyName %></h1>
          </div>

          <!-- Content -->
          <div style="background-color: #ffffff; padding: 24px; border-radius: 8px; margin: 20px 0;">
            <%- body %>
          </div>

          <!-- Footer -->
          <div style="text-align: center; padding: 20px 0; color: #6b7280; font-size: 14px;">
            <p style="margin: 0 0 10px 0;">
              © <%= new Date().getFullYear() %> <%= companyName %>. All rights reserved.
            </p>
            <p style="margin: 0;">
              <a href="<%= unsubscribeUrl %>" style="color: #6b7280; text-decoration: underline; margin-right: 10px;">Unsubscribe</a>
              <a href="<%= privacyUrl %>" style="color: #6b7280; text-decoration: underline;">Privacy Policy</a>
            </p>
          </div>
        </div>
      </td>
    </tr>
  </table>
</body>

</html>